export type CategoryLayout = 'slider' | 'grid';

export interface ScentCategory {
  id: string;
  name: string;
  description: string;
  image: string;
  color: string;
  icon: string;
  link: string;
}
