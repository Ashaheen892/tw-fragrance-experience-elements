export type IngredientLayout = 'grid' | 'list';

export interface IngredientStory {
  id: string;
  name: string;
  shortTeaser: string;
  story: string;
  origin: string;
  character: string;
  mood: string;
  image: string;
  color: string;
  icon: string;
}
